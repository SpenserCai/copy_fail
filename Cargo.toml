[workspace]
members = [
    "crates/exp",
    "crates/copy_fail_guard",
]
resolver = "3"

# The eBPF crate (crates/copy_fail_guard-ebpf) is NOT a workspace member.
# It targets bpfel-unknown-none and must be compiled separately with bpf-linker.
